import React from 'react';
import AdminContainer from "@/components/container/AdminContainer";

const Upload = () => {
    return (
        <AdminContainer>Upload</AdminContainer>
    )
};

export default Upload;