import React from 'react';
import AdminContainer from "@/components/container/AdminContainer";

const Invoice = () => {
    return (
        <AdminContainer>Invoice</AdminContainer>
    )
};

export default Invoice;