import React from 'react';
import AdminContainer from "@/components/container/AdminContainer";

const Slider = () => {
    return (
        <AdminContainer>Slider</AdminContainer>
    )
};

export default Slider;