import React from 'react';
import AdminContainer from "@/components/container/AdminContainer";

const Subscription = () => {
    return (
        <AdminContainer>Subscription</AdminContainer>
    )
};

export default Subscription;