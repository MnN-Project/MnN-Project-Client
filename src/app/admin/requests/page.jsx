import React from 'react';
import AdminContainer from "@/components/container/AdminContainer";

const Requests = () => {
    return (
        <AdminContainer>Requests</AdminContainer>
    )
};

export default Requests;