'use client';
import React from 'react';
import AdminContainer from '@/components/container/AdminContainer';

const Users = (props) => {
  return <AdminContainer>Users List</AdminContainer>;
};

export default Users;
